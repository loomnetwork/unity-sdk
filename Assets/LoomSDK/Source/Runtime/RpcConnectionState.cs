namespace Loom.ClientSdk
{
    public enum RpcConnectionState
    {
        Undefined = 0,
        Connecting,
        Connected,
        Disconnecting,
        Disconnected,
        NonApplicable
    }
}
