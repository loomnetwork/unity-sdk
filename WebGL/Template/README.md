# Loom DAppChain SDK - WebGL Template Code

## Overview

This directory contains code that's used to generate the `Assets/WebGLTemplates/Loom/bundle.js`.

## Development

```bash
# first time setup
yarn install

# generate bundle.js
yarn build
```
